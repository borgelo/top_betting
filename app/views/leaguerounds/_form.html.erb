<%= form_for(@leagueround) do |f| %>
  <% if @leagueround.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leagueround.errors.count, "error") %> prohibited this leagueround from being saved:</h2>

      <ul>
      <% @leagueround.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
