	<div class="row">
		<div class="span1">
		</div>
		<div class="span2">
			<h4><%= link_to 'Resultat', bets_path %></h4>
			<h4><%= link_to 'Tabell', leagues_path %></h4>
			<h4><%= link_to 'Resultat 2016', bets_path(:season => "2016") %></h4>
			<h4><%= link_to 'Tabell 2016', leagues_path(:season => "2016") %></h4>
			<h4><%= link_to 'Resultat 2014', bets_path(:season => "2014") %></h4>
			<h4><%= link_to 'Tabell 2014', leagues_path(:season => "2014") %></h4>
		</div>
		<div class="span6 well lead">
			<table class="table table-condensed table-hover">
				<thead>
					<tr><th>Navn</th><th>&nbsp;</th><th>Poeng</th></thr>
				</thead>
				<% @users_sorted.each do |user|
					if user.hasBets
				%>
					<tbody>
						<tr>
							<td class="lead"><%= user.name %></td>
							<td>&nbsp;</td>
							<td class="lead"><%= user.points %></td>
						</tr>
					</tbody>
				<% end
				 end%>
			</table>
		</div>
	</div>

	<% @users_sorted.each do |user|
		if user.hasBets
	%>

	<div class="row">
		<div class="span3">
		</div>
		<div class="span6 well lead">
			<table class="table table-condensed table-hover">
				<thead>
					<tr>
						<th align="left" colspan=3><%= user.name %></th>
					</tr>
				</thead>
				<tbody>
				<% user.bets.each do |bet|
					if (bet.seasonstartyear == @useSeason)
				%>
					<tr>
						<td class="lead"><%= bet.position %>.</td>
						<td class="lead"><%= bet.league.name %></td>
						<td class="lead"><%= bet.points %></td>
					</tr>
				<% end
				   end %>
					<tr>
						<th class="lead">Sum</th>
						<th class="lead">&nbsp;</th>
						<th class="lead"><%= user.points %></th>
					</tr>
				</tbody>
			</table>
		</div>
	</div>

		<%
		   end
		   end
		%>
		
	<div style="text-align:center"><%= link_to 'Tabell', leagues_path %></div>
	<div style="text-align:center"><%= link_to 'Tabell 2016', leagues_path(:season => "2016") %></div>
	<div style="text-align:center"><%= link_to 'Tabell 2014', leagues_path(:season => "2014") %></div>




